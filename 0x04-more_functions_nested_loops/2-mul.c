
#include "main.h"
/**
 * mul - checks for checks for a digit (0 through 9).
 * @v: v - Variable
 * @c: c - variable
 * Return: Always 0.
 */
int mul(int c, int v)
{
 int mul;

 mul = c * v;

 return (mul);
}

